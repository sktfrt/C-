# PRACTIC 8
## containers
`cppreference/containers`  
`auto` наше все

## hm
1) класс - парсер аргов, `ArgParser(argv, argc)`
2) enumчики
3) применяем ВСЕ что знаем(свобода как на ооп)
4) исключение с нормальным сообщением, без фолов
5) нормальный вывод ошибок
6) грамотная очистка ресурсов(вектор, шаред птр и тд)
7) максимум приватности
8) final ы
9) экономия ресурсов
10) буфер для считывания
11) очередь с приоритетами, `std::priority_queue`
12) побольше примеров
13) разбиение на файлы и namespace


`HuffmanAlgo()` + табличка частот, потоки, дерево 
```cpp
HuffmanTree()
    unique_ptr HuffmanNode root;
    class HuffmanNode;
```

1) юнитесты (полное тестиование) - `doctest`
2) функциональное тестирование
3) проверка исключений
4) CHECK_THROWS_WITH_AS
   
```cpp
assert(test_algo_compress(HuffmanAlgo));
```
