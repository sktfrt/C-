# PRACTIC 14
## filesystem C++17
`std::path`  

## std::ranges and concepts
`std::views`  

## async n coroutines
`std::threads`  
`std::future`  
`std::promise`  

почитать про функции-генераторы 

`co_return`  
`co_yeild`  
`co_await`  

OpenMP - библиотека для многопоточки, ващ корутины фигня в плюсах, их ващ не используют